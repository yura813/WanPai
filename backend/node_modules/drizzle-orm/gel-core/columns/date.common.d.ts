import type { ColumnBuilderBaseConfig, ColumnDataType } from "drizzle-orm/column-builder";
import { entityKind } from "drizzle-orm/entity";
import { GelColumnBuilder } from "drizzle-orm/gel-core/columns/common";
export declare abstract class GelLocalDateColumnBaseBuilder<T extends ColumnBuilderBaseConfig<ColumnDataType, string>, TRuntimeConfig extends object = object> extends GelColumnBuilder<T, TRuntimeConfig> {
    static readonly [entityKind]: string;
    defaultNow(): import("drizzle-orm/column-builder").HasDefault<this>;
}
