import type { Check } from "drizzle-orm/sqlite-core/checks";
import type { ForeignKey } from "drizzle-orm/sqlite-core/foreign-keys";
import type { Index } from "drizzle-orm/sqlite-core/indexes";
import type { PrimaryKey } from "drizzle-orm/sqlite-core/primary-keys";
import { SQLiteTable } from "drizzle-orm/sqlite-core/table";
import { type UniqueConstraint } from "drizzle-orm/sqlite-core/unique-constraint";
import type { SQLiteView } from "drizzle-orm/sqlite-core/view";
export declare function getTableConfig<TTable extends SQLiteTable>(table: TTable): {
    columns: import("drizzle-orm/sqlite-core").SQLiteColumn<any, {}, {}>[];
    indexes: Index[];
    foreignKeys: ForeignKey[];
    checks: Check[];
    primaryKeys: PrimaryKey[];
    uniqueConstraints: UniqueConstraint[];
    name: string;
};
export type OnConflict = 'rollback' | 'abort' | 'fail' | 'ignore' | 'replace';
export declare function getViewConfig<TName extends string = string, TExisting extends boolean = boolean>(view: SQLiteView<TName, TExisting>): {
    name: TName;
    originalName: TName;
    schema: string | undefined;
    selectedFields: import("drizzle-orm").ColumnsSelection;
    isExisting: TExisting;
    query: TExisting extends true ? undefined : import("drizzle-orm").SQL<unknown>;
    isAlias: boolean;
};
