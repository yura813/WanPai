import type { Dialect } from "drizzle-orm/column-builder";
export interface RunnableQuery<T, TDialect extends Dialect> {
    readonly _: {
        readonly dialect: TDialect;
        readonly result: T;
    };
}
